spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/boarddb
    username: root
    password: ys1234
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
