spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1  # 인메모리 데이터베이스
    driver-class-name: org.h2.Driver
    username: sa
    password: test1234
  jpa:
    hibernate:
      ddl-auto: create-drop  # 테스트 후 DB 스키마를 삭제
    show-sql: true
